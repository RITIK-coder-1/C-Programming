// Calculate the cube of a number ------->

#include <stdio.h>

int main(){
    printf("Enter a number: ");
    int num;
    scanf("%d", &num);
    printf("The Cube of the number is: %d\n", num * num * num);
    return 0;
}